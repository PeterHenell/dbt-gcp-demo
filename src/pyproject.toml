[tool.poetry]
name = "peter-dataproduct"
version = "0.1.0"
description = ""
authors = [""]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.9,<3.11"
dbt-core = "^1.7"
dbt-bigquery = "^1.7"



[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
